apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
spec:
  type: {{ .Values.service.type | default "LoadBalancer" }}
  ports:
  - name: result
    port: 80
  selector:
    app: result

